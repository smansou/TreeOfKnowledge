# PWA notes

- Service worker is mocked in dev via MSW; the real SW handles offline shell + caching
- Manifest icons updated (maskable variants)
- Install prompt deferred until after first interaction
